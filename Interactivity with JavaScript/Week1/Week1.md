# DOM Review with Object Oriented Programming

## DOM
document -the root of the page

## node 

every element in DOM is a node, which have attributes

## nodeList

an array(group) of elements

## How javascript interact with DOM?

API

> ? document.getElementById(id)  
? document.getElementsByClassName(class)  
? element.innerHTML  
? element.style  
? element.setAttribute(attribute, value)  
? element.removeAttribute(attribute)  

# Output

JavaScript doesn＊t have a built-in
print function

Data is displayed via
每 an alert box using ```window.alert()```, ```alert()```   
每 a prompt using ```window.prompt()```, ```prompt()```   
每 HTML output using ```document.write()```  
每 HTML element using ```innerHTML()```  
每 the browser console using ```console.log()```   



what's the diffierence between ```alert``` and ```prompt```, ```write``` and ``innerHTML``, and ```console.log```?

```alert``` and ```prompt``` - popup box 
*   alert - output
*   prompt - output and input    

```write```, ```innerHTML``` - document  
```console.log``` - console

